apiVersion: v1
kind: ConfigMap
metadata:
  name: hasher-config
  namespace: default
  labels:
    app: nginx-webapp
data:
  PID_NAME: "nginx"
  MAIN_CONTAINER_NAME: "nginx"
  MOUNT_PATH: "etc/nginx/nginx.conf"